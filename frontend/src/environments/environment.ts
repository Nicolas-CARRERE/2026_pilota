/**
 * Development environment.
 * apiUrl must match the Node.js API server (default port 3000, or PELOTA_API_SERVER_PORT).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
